package com.tikal.workshop

import scala.collection.mutable.ListBuffer

object Average {
  
  def main(args : Array[String]) : Unit = {
    
    val listOfNumbers = List(10, 12, 4, 5, 8, -4);

    // what is the average of these numbers?
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    println("Average: " + listOfNumbers.sum / listOfNumbers.size ) ;
//    println (35 / 6);
//    val listOfDoubles = listOfNumbers.map { x : Int => x.toDouble }
//    println("Average: " + listOfDoubles.sum / listOfDoubles.size ) ;
//    println("Average: " + listOfNumbers.sum.asInstanceOf[Double] / listOfNumbers.size ) ;
    
  }
}